# ? Application Name
spring.application.name=taxcertify

# ? Server Settings
server.port=8080
# server.servlet.context-path=/taxcertify

# ? MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/taxcertify_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Bangkok
spring.datasource.username=devuser
spring.datasource.password=@!Infosysasia2012
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ? JPA / Hibernate
# ??? update ?????? dev
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ? Timezone / Locale
spring.jackson.time-zone=Asia/Bangkok
spring.mvc.format.date=dd/MM/yyyy
spring.mvc.format.date-time=dd/MM/yyyy HH:mm

# ? Logging (Optional: ?????? debug SQL)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE
